"""GridLens — trustworthy GB electricity analytics.

A deterministic engine that computes carbon-intensity and generation-mix metrics
from the official Carbon Intensity API, validates them against the source's own
figures, and renders a self-contained dashboard.
"""

__version__ = "0.1.0"
